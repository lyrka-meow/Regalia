name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7
      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false
      - name: Format check
        run: test -z "$(gofmt -l .)"
      - name: Test
        run: go test ./...
      - name: Check installer scripts
        run: bash -n installer/*.sh packaging/github/*.sh
      - name: Build
        run: |
          go build ./cmd/regalia
          go build ./cmd/regalia-engine
          go build ./cmd/regaliad
      - name: Build release payload
        run: packaging/github/build-binary-release.sh ci x86_64
